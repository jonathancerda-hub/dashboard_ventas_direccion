# =====================================================
# CONFIGURACIÓN DEL DASHBOARD DE VENTAS
# =====================================================

# -----------------------------------------------------
# FLASK
# -----------------------------------------------------
SECRET_KEY=tu_secret_key_aqui_cambiar_en_produccion
ADMIN_USERS=user1@example.com,user2@example.com

# -----------------------------------------------------
# SUPABASE (Base de datos históricos)
# -----------------------------------------------------
SUPABASE_URL=https://tu-proyecto.supabase.co
SUPABASE_KEY=tu_supabase_anon_key_aqui

# Habilitar caché en memoria (solo para desarrollo con >2GB RAM)
# En Render Free Tier (512 MB) usar: false
# En desarrollo local con >2GB RAM usar: true
ENABLE_SUPABASE_CACHE=false

# -----------------------------------------------------
# ODOO (ERP)
# -----------------------------------------------------
ODOO_URL=https://tu-odoo.com
ODOO_DB=nombre_base_datos
ODOO_USERNAME=usuario@example.com
ODOO_PASSWORD=tu_password_odoo

# -----------------------------------------------------
# GOOGLE SHEETS (Metas de ventas)
# -----------------------------------------------------
# Formato: JSON escapado en una sola línea o archivo credentials.json
GOOGLE_CREDENTIALS_JSON='{"type": "service_account", "project_id": "...", ...}'
# O usar archivo local:
# GOOGLE_CREDENTIALS_FILE=credentials.json

# -----------------------------------------------------
# PRODUCCIÓN (Render.com / Heroku / etc)
# -----------------------------------------------------
# Estas variables se configuran en el dashboard del hosting:
# - SECRET_KEY (generar nuevo valor único)
# - ADMIN_USERS (lista de emails con acceso)
# - SUPABASE_URL y SUPABASE_KEY
# - ODOO_URL, ODOO_DB, ODOO_USERNAME, ODOO_PASSWORD
# - GOOGLE_CREDENTIALS_JSON
# - ENABLE_SUPABASE_CACHE=false (importante para Free Tier)
